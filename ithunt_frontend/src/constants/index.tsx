export enum USER_STATUS {
  ON = "on",
  OFF = "off",
}
export enum USER_ROLE {
  STU = "student",
  ADMIN = "admin",
}

export enum QUESTION_TYPE {
  MUL = "multiple choices",
  TF = "true-false",
  FREE_TEXT = "free text",
}
